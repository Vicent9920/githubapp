class DynamicData {
  final String display_login;
  final String avatar_url;
  final String name;
  final String created_at;

  DynamicData( this.display_login, this.avatar_url, this.name, this.created_at);
}
